import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class DurakTest {

	@Test
	void test() {
		fail("Not yet implemented");
		Durak d = new Durak();
		assertTrue(d.checkWinningConditions());
		assertTrue(d.computersCardsAreHighSuit());
		assertTrue(d.computersFirstTurn());
		assertTrue(d.computersTurn());		
	}

}
